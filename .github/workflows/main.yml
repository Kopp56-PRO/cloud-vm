name: Kopp56's RDP

on:
  workflow_dispatch:

jobs:
  build:
    name: K56 DEV's RDP
    runs-on: windows-latest
    steps:
      - name: Downloading & Installing Essentials
        run: |
          Invoke-WebRequest -Uri "https://www.dropbox.com/scl/fi/budug1p677wj227wx0w80/Downloads.bat?rlkey=fqqa7cxi634lfa1lck8llm7el&st=2du955k2&dl=1" -OutFile "Downloads.bat"
          cmd /c Downloads.bat

      - name: Log In
        run: cmd /c show.bat

      - name: Keep running
        run: python time.py
